## What is the difference between retrieval and generation?

Retireval is component that returns most relevant chunks from the knowledge base,Whereas Generation is using the retrieved chunks which are similar and generating a response or an answer to the query.
